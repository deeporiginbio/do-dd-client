---
description: Python code
globs: *.py
alwaysApply: true
---

# Code Style

- Use type hints for all function arguments and return types
- Use f-strings for string formatting
- Prefer string literals over Enums
- Prefer union types using `|` to the `Union` type
- Type lists and dicts using `list` and `dict` (lowercase)
- Force kwargs using * when a function has more than 3 arguments, or if arguments have the same type
- Write pytest-style functions for tests. Don't use unittest. Use fixtures if needed. 
- Use Google-style docstrings
- Every function must have a docstring
- Every module must have a docstring 
- Prefer dict literals `{"foo": "bar"}` to `dict(foo="bar")`
- When defining new constants or literals, store them in src/utils/constants.py

# Writing a new function

When writing a new function, make the following three changes together:

1. Write the new function. Follow code style
2. Write tests for that function in tests/. Follow test code style
3. Write documentation for that function in docs/

Run that test to ensure the function you wrote works


# Editing an existing function

When modifying an existing function, make the following changes together:

1. Modify the function as needed. Follow code style
2. Update tests for that function and verify that tests pass
3. Update documentation if needed. 

# Running tests

- Before running tests, run `uv run ruff format .` and `uv run ruff check --select I . --fix` to ensure that code is formatted correctly
- Run tests by using `uv run pytest --env local -x`. Fix failing tests. 
- To test a specific function, use `uv run pytest -k --env local` 

# Delivering new SDK features

When shipping a new user-facing SDK capability in the cli repo, include all of the following:

1. Implementation following code style
2. Tests in `tests/` that verify the behavior (mock server routes when needed)
3. Documentation in `docs/` (API reference and/or how-to as appropriate)
4. **Demo notebook** — never create or edit notebooks in `docs/notebooks/clean/` directly. Write `docs/notebooks/dirty/{feature}.ipynb` (gitignored; agents can still create it). After it runs against a live org (`deeporigin login`, `DeepOriginClient.from_disk()`), promote with `./scripts/notebooks.sh` (dirty → clean, strip outputs). The committed artifact is `docs/notebooks/clean/{feature}.ipynb`.

# Common mistakes

- Editing `src/VERSION` or `platform-sdk/src/VERSION` — never change these files; release CI sets the version from the git tag (see `.github/workflows/pypi.yml` and `.github/workflows/pypi_sdk.yml`)
- Using uppercase List instead of lowercase list to annotate the type of a list
- Using uppercase Dict instead of lowercase dist to annotate the type of a dict
- Not using `uv run` to run some code within the correct venv
- Creating or editing notebooks under `docs/notebooks/clean/` — use `docs/notebooks/dirty/` and `./scripts/notebooks.sh` instead